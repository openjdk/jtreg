### Section clean
rm -f %WS%/build/RerunTest.agentvm/work/classes/std/CleanTest.class

### Section clean
for f in %WS%/build/RerunTest.agentvm/work/classes/std/p/*; do
  if [ -f $f ]; then rm $f ; fi
done

