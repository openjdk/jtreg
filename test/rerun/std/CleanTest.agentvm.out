### Section clean
rm -f %BUILD%/RerunTest.agentvm/work/classes/std/CleanTest.class

### Section clean
for f in %BUILD%/RerunTest.agentvm/work/classes/std/p/*; do
  if [ -f $f ]; then rm $f ; fi
done

