### Section clean
rm -f %WS%/build/RerunTest.othervm/work/classes/std/CleanTest.class

### Section clean
for f in %WS%/build/RerunTest.othervm/work/classes/std/p/*; do
  if [ -f $f ]; then rm $f ; fi
done

